错误原因分析：
报错 `could not translate host name` 是因为 Supabase 的直连地址 (`db.gzuldfrwklbeuxypmdzk.supabase.co`) 是 **IPv6** 的，而您的网络环境或 Python 环境只支持 IPv4，导致无法解析。

**解决方案：切换到 IPv4 兼容的连接池模式**

根据 IP 归属地分析，您的数据库位于 **东京 (ap-northeast-1)** 区域。我将为您自动配置兼容 IPv4 的连接地址：

1.  **修改配置 (`.streamlit/secrets.toml`)**：
    *   **主机 (Host)**：改为 `aws-0-ap-northeast-1.pooler.supabase.com` (AWS 东京节点的连接池地址)。
    *   **用户名**：改为连接池专用格式 `postgres.gzuldfrwklbeuxypmdzk`。
    *   **端口**：改为 `6543` (事务模式) 或 `5432` (会话模式)。我会先尝试 `5432`。
2.  **重启服务**：应用更改。

这样应该能解决连接问题，无需您手动查找配置。