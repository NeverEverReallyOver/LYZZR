The user reports that after chatting with someone, their score isn't being reflected in the "Candidate Pool" (Square) UI, where it should show "✅ 已聊过 (XX分)".

**Analysis:**

1.  **Data Persistence**: We confirmed that both batch and single chats now save to the DB (`match_records`).
2.  **Data Retrieval**:
    *   In `app.py` (line 169), `chatted_map` is fetched: `chatted_map = storage.get_chatted_users(current_user.user_id)`.
    *   This map is used in the loop (line 191) to check `if candidate.user_id in chatted_map`.
3.  **The Issue**: `chatted_map` is fetched **before** the new chat happens.
    *   When a single chat ends, `st.rerun()` is called (line 390).
    *   This re-runs the entire script. `chatted_map` *should* be re-fetched with fresh data.
    *   However, if `storage.get_chatted_users` uses caching (TTL), it might return stale data.
    *   Looking at `src/storage.py`, `get_chatted_users` uses `ttl=0`, so caching shouldn't be the issue *unless* Streamlit's cache invalidation is tricky or the query itself is wrong.
    *   **Wait**, look at `get_chatted_users` in `src/storage.py`:
        ```python
        sql = """
            SELECT user_a, user_b, match_score FROM match_records 
            WHERE user_a = :u OR user_b = :u
        """
        ```
    *   And the logic:
        ```python
        partner = row['user_b'] if row['user_a'] == username else row['user_a']
        ```
    *   This logic assumes that if `user_a` is me, `user_b` is the partner. This is correct.
    *   BUT, for virtual users, their ID is like `guest_12345`.
    *   The `candidate_pool` in `app.py` generates *new* virtual users with *new* IDs every time if they are not persisted!
    *   **Crucial Logic in `app.py` (lines 135-156)**:
        *   `real_candidates` are fetched from DB.
        *   `virtual_candidates` are generated on the fly: `v_id = f"guest_{int(time.time())}_{i}"`.
        *   **Problem**: Every time `main()` runs (including `st.rerun()`), **new virtual users with new IDs** are created!
        *   So even if you chatted with `guest_170000_0`, after rerun, the pool has `guest_170001_0`. The old ID is gone from the pool, so `if candidate.user_id in chatted_map` will always be False for virtual users because the IDs in the pool are brand new.

**Solution:**

The issue is that virtual users are ephemeral. To fix this, we need to persist virtual users or make their IDs deterministic/stable for the session.

**Option A (Session Persistence)**: Store the `candidate_pool` in `st.session_state` and only regenerate if explicitly requested or if the pool is empty.
*   `app.py` already tries to do this:
    ```python
    if st.session_state.candidate_pool is None:
        # ... fetch and generate ...
        st.session_state.candidate_pool = real_candidates
    ```
*   So `candidate_pool` *should* be stable across reruns.
*   **However**, in `app.py` line 92 (`if st.sidebar.button("登出"): ... st.session_state.candidate_pool = None`), it clears it on logout.
*   But look at the "Batch Chat" logic: `st.rerun()` is called.
*   Look at "Single Chat" logic: `st.rerun()` is called.
*   When `st.rerun()` happens, `st.session_state.candidate_pool` *should* persist.
*   **Wait**, check line 108: `if st.session_state.candidate_pool is None:`.
*   If `candidate_pool` is NOT None, it uses the existing one.
*   So the IDs *should* remain the same.

**Let's re-examine `chatted_map` logic**:
*   `chatted_map` is fetched at line 169.
*   If I chat with `guest_A`, `match_records` has `guest_A`.
*   `chatted_map` has `guest_A`.
*   `candidate_pool` (from session state) has `guest_A`.
*   The check `if candidate.user_id in chatted_map` should pass.

**Is there a bug in `storage.get_chatted_users`?**
*   Maybe the `user_id` types don't match? (Str vs Int?) IDs are strings.
*   Maybe the query `get_chatted_users` is failing silently?

**Alternative Theory**:
When we do `real_candidates.extend(virtual_candidates)`, we are modifying the list in place.
If `st.session_state.candidate_pool` is saved, it contains the `guest_` users.
Is it possible `st.session_state.candidate_pool` is being reset somewhere?
*   Line 102: `if 'candidate_pool' not in st.session_state: st.session_state.candidate_pool = None`.
*   Line 92 (Logout): Sets to None.
*   Nowhere else seems to reset it.

**Wait**, I see `if search_query: candidates = [...]`. This creates a new list but elements are same objects.

**Let's look at `storage.py` again.**
`get_chatted_users` returns `result[partner] = score`.
The query: `SELECT user_a, user_b, match_score FROM match_records ...`
If I am `admin`, and I chat with `guest_1`, `user_a`=`admin`, `user_b`=`guest_1`.
Row: `admin, guest_1, 80`.
Logic: `partner` = `guest_1`. `result` = `{'guest_1': 80}`.
In `app.py`: `if candidate.user_id in chatted_map`. `candidate.user_id` is `guest_1`.
This *should* work.

**Maybe the issue is where `chatted_map` is called?**
It is called at line 169.
`st.rerun()` re-executes the whole script.
So `chatted_map` is refreshed.

**Wait, what if `save_match_record` failed silently?**
I added `storage.save_match_record` in the previous step.
Did I commit it? Yes.

**Is it possible the user is referring to "Real Users"?**
If I chat with a real user, `user_b` is their username.
The logic holds.

**Let's check the SQL query in `get_chatted_users` again.**
```python
sql = """
    SELECT user_a, user_b, match_score FROM match_records 
    WHERE user_a = :u OR user_b = :u
"""
```
If `user_a` is `Alex`, `user_b` is `Bob`.
Wait, `match_records` might have multiple records for the same pair (if they chat multiple times).
The python logic:
```python
if partner not in result or score > result[partner]:
    result[partner] = score
```
This takes the *max* score. This is correct.

**Hypothesis: The virtual user ID generation.**
`v_id = f"guest_{int(time.time())}_{i}"`
`int(time.time())` changes every second.
If `st.session_state.candidate_pool` is **NOT** persisting correctly, then IDs change.
But it is in `session_state`.

**Wait!**
In `app.py`, line 135:
```python
if st.session_state.candidate_pool is None:
    # ... generate ...
    st.session_state.candidate_pool = real_candidates
```
This block only runs if pool is None.
So if I refresh (rerun), it skips this block and uses the old pool.
So the IDs are stable.

**What if the user is clicking "Matches" (Rankings) and expecting it to update immediately *without* a rerun?**
But `save_match_record` is followed by `st.rerun()`.

**Maybe I missed a spot?**
Ah, in `app.py`, look at where `chatted_map` is used.
It's used in the `with st.form("batch_match_form"):` loop.
If I am in "Single Chat" mode (`st.session_state.chat_active` is True), the "Square" (Candidate Pool) is NOT visible?
No, the Square is rendered at line 127.
The Chat Room is rendered at line 343.
They are both rendered?
Yes, `app.py` renders them sequentially.
1. Square
2. Batch Progress (if active)
3. Chat Room (if active)
4. Leaderboard (if active)

If I am in Chat Room, `chat_active` is True.
The Square is still rendered above it.
If I finish the chat, `st.rerun()` happens.
`chat_active` is still True?
In `app.py`:
```python
if st.button("🔄 再来一次"):
    st.session_state.chat_active = False
    st.rerun()
```
When chat finishes, `st.rerun()` is called. `chat_active` is still True (because we didn't set it to False).
So the page reloads.
1. Square renders. `chatted_map` is fetched. It *should* contain the new record.
2. Chat Room renders.

**Wait, I found a potential issue.**
In `storage.py`, `save_match_record` uses `json.dumps(chat_log, ensure_ascii=False)`.
But in the previous step, I fixed the `TypeError` by checking if it's a string.
Maybe the save is failing?

**Let's look closely at `get_chatted_users` in `src/storage.py` again.**
It returns a `dict`.
`st.info(f"✅ 已聊过 ({score}分)")` uses this dict.

**Maybe the problem is `candidate.user_id`.**
For real users, `user_id` is `username`.
For virtual users, `user_id` is `guest_...`.
In `match_records` table:
`user_a` is `current_user.user_id` (username).
`user_b` is `target.user_id`.

**Let's try to debug by adding a log or toast to see what `chatted_map` contains.**
Or better, let's fix a potential issue in `app.py`:
In the "Single Chat" block, when chat finishes:
```python
# ... save record ...
st.rerun()
```
After `st.rerun()`, `chat_active` is still True.
The user sees the chat room with the report.
The user might scroll up to the Square.
They should see the score.

**Wait! The `candidate_pool` persistence might be the problem.**
If the user code is reloading `app.py`, `st.session_state` is preserved.
BUT, `CandidateGenerator` imports `random`.
The IDs are generated with `time.time()`.
If the session state IS lost (e.g. strict refresh), IDs change.
But `st.rerun()` preserves session state.

**Let's assume the user is right.**
If "already chatted score is not added", it implies `candidate.user_id` is NOT in `chatted_map`.
This means:
1. The record wasn't saved.
2. The record was saved with a different ID.
3. The query didn't return it.

**Let's check `save_match_record` again.**
```python
sql = text("""
    INSERT INTO match_records (user_a, user_b, chat_log, match_score, report)
    VALUES (:user_a, :user_b, :chat_log, :score, :report)
""")
```
This looks correct.

**Is it possible that `current_user.user_id` is not what we think?**
`current_user` is loaded from `st.session_state`.

**What if `get_chatted_users` fails?**
I should add some debugging or ensure `storage.py` is robust.

**Actually, I suspect `match_score` might be float vs int.**
In `app.py`, `score` is fetched as `total_score` (int).
In `get_chatted_users`, it reads `match_score`.
This shouldn't be an issue for dictionary lookup.

**Let's look at the `app.py` logic for `st.info` again.**
```python
if candidate.user_id in chatted_map:
    score = chatted_map[candidate.user_id]
    st.info(f"✅ 已聊过 ({score}分)")
```

**Wait! I found a subtle bug in `save_match_record` call I added.**
I added:
```python
storage.save_match_record(current_user.user_id, agent_b.user_id, formatted_history, score, summary)
```
`agent_b` is `st.session_state.selected_candidate`.
This is an `AgentProfile` object.
`agent_b.user_id` is correct.

**Let's verify `get_chatted_users` implementation in `storage.py`.**
It catches generic Exception and returns `{}`.
If there's an SQL error, it returns empty.
Maybe `match_records` table schema has a constraint?
`match_score` type?
If `total_score` from evaluator is a float, and DB expects int?
Or if `report` text is too long?

**Plan:**
1.  **Add Error Logging**: I will modify `storage.py` to print the exact error in `get_chatted_users` instead of just returning empty, so we can see if it's failing.
2.  **Cast Score**: In `app.py`, explicitly cast score to `int()` before saving, just in case.
3.  **Force Refresh**: In `app.py`, right before rendering the Square, I will explicitly call `storage.get_chatted_users` again to ensure it's fresh (though it already is).
4.  **Check Virtual User Persistence**: I will modify `app.py` to ensure virtual users have stable IDs even if the pool is regenerated (though session state should handle this). I'll change the ID generation to use a deterministic hash or just index if possible, or trust session state.

**Actually, there is a clearer potential issue.**
In `src/storage.py`, `get_chatted_users` uses `ttl=0` (no cache).
But `st.connection("supabase", type="sql")` might have its own caching behavior if not configured correctly.
Wait, `ttl=0` means "don't cache".

**Let's try to fix the Score Type.**
The `total_score` from LLM might be a string "85" or int 85.
DB might be strict.
I will ensure it's an int.

**Also, I will make the virtual user IDs more stable.**
Instead of `time.time()`, I will use `uuid` or just simple indexing `guest_{i}` but that might conflict if multiple users.
Actually, `guest_{int(time.time())}_{i}` is fine as long as `candidate_pool` is preserved.

**Wait, I see the issue!**
In `get_chatted_users`:
```python
df = self.conn.query(sql, params={"u": username}, ttl=0)
```
If `df` is empty, it returns `{}`.
If `df` has rows, it iterates.
`partner = row['user_b'] ...`
If `user_a` and `user_b` columns are not what we expect?
`user_a` is VARCHAR, `user_b` is VARCHAR.

**Let's apply the fix:**
1.  In `app.py`, ensure `score` passed to `save_match_record` is `int`.
2.  In `storage.py`, `get_chatted_users` print error details.
3.  **Most likely cause**: The `chatted_map` keys might be matching types (str) correctly, but maybe the user ID in `candidate_pool` has extra whitespace or something? Unlikely.

**Another possibility**:
The user says "Scores not added to leaderboard".
Wait, the user said "Leaderboard" in the prompt, but the context implies "Square" (Candidate Pool).
"Already chatted score not added to leaderboard" -> "已经聊过的分数没有加到排行榜啊"
If they mean the **Sidebar Leaderboard**, I just fixed the LEFT JOIN.
If they mean the **Square Status**, that's `chatted_map`.

The user previously said: "Now that we have the leaderboard, if I chat with someone, the result doesn't show ... nor update to the leaderboard".
I fixed the "save to DB" part.
Now they say "Already chatted scores not added to leaderboard".
If they mean the sidebar: I fixed the JOIN issue.
Maybe they mean **scores from BEFORE the fix**?
Those records are lost (not saved).
Or maybe they mean **Virtual Users** are still missing?
I fixed `get_match_history` with LEFT JOIN.

**Wait, did I execute the LEFT JOIN fix?**
Yes, `fix_leaderboard_join` was completed.

**Maybe the user means: "I chatted, but the score is not UPDATING in the leaderboard?"**
If they chat again with the same person, `save_match_record` inserts a NEW record.
`get_match_history` sorts by `match_score DESC`.
So the highest score should show up.

**Let's assume the user is talking about the Candidate Square.**
"已经聊过的分数" -> "Scores of those I've chatted with".
"没有加到排行榜" -> "Are not added to the leaderboard".
This phrasing is ambiguous. It could mean "The score badge in the square is missing" OR "The sidebar leaderboard is missing them".

**Let's cover both bases.**
1.  **Sidebar Leaderboard**: Ensure the `LEFT JOIN` fix is working and `COALESCE` is correct.
2.  **Square Status**: Ensure `chatted_map` works.

**I suspect one more thing in `storage.py`:**
In `get_match_history`, I used `COALESCE(u.name, 'AI Guest')`.
But in the `candidate_pool`, the virtual user has a specific name (e.g. "Jessica").
If the leaderboard shows "AI Guest" instead of "Jessica", the user might think it's broken.
**We need to recover the name for virtual users.**
The name is NOT in the `users` table.
BUT, it IS in the `chat_log` stored in `match_records`.
However, parsing JSON in SQL is painful/slow.
**Better solution**: When saving the match record, we should save the **partner's name** in the `match_records` table too!
Currently `match_records` schema is:
`user_a, user_b, chat_log, match_score, report`
It does NOT have `partner_name`.
Adding a column requires schema migration.

**Alternative**: In `get_match_history` (Python side), if `partner_name` is "AI Guest" (or NULL), try to parse it from `chat_log`.
`chat_log` is a JSON string.
We can load it, find the partner's name.

**Plan:**
1.  **Modify `storage.py`**:
    *   In `get_match_history`, iterate through the results.
    *   If `partner_name` is 'AI Guest' (from the SQL Coalesce), try to extract the real name from `chat_log`.
    *   The `chat_log` is a list of messages. The partner's name is usually the one that isn't the current user.
    *   Or, just look at `chat_log[0]['name']` (System) or `chat_log[1]['name']`...
    *   Actually, `match_records` saves `user_b`.
    *   We can't easily know the name unless we parse the log.
    *   Let's try to extract it from the log.

**Refined Plan**:
1.  Update `get_match_history` in `storage.py`.
2.  After fetching `df`, convert to dict.
3.  Loop through records.
4.  If `partner_name` is "AI Guest":
    *   Parse `chat_log`.
    *   Identify the partner's name. (The partner is the one who isn't `user_a`... wait, `chat_log` has `name` field).
    *   We can just take the name from the first message that is NOT "System" and NOT `user_a`'s name (we know `user_a`'s name? No, we know `user_a` ID).
    *   Actually, `chat_log` contains names.
    *   We can simply scan `chat_log` for a name that corresponds to `user_b`? No, `user_b` is an ID.
    *   We can just pick the name that appears as a sender `role='assistant'`? No, `role` might be user/assistant.
    *   In `DialogAgent`, `name` is saved.
    *   So we just find the name that isn't the current user's name.
    *   Wait, `get_match_history` takes `username` (current user ID).
    *   We can find the user's name from `users` table, but we might not have it handy in this function.
    *   **Heuristic**: Just look for the first message with `role='assistant'` (assuming bot is assistant? No, in 1v1 both are agents).
    *   Look at `chat_log`. It has `name` field.
    *   If we saved the chat log correctly, it has names.
    *   We can just pick the name that is NOT the current user's name. But we don't know current user's NAME, only ID.
    *   However, usually one name is "Me" or the user's name.
    *   Let's just take the `name` from the `chat_log` where `name != 'System'`. There should be two names.
    *   If we can't determine, keep "AI Guest".

This will make the leaderboard look correct for virtual users ("Jessica" instead of "AI Guest").
