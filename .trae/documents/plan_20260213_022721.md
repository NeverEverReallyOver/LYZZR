这简直是“找茬”游戏的高级版！`ReActAgent` 居然也不接受 `model_config_name` 参数？这不科学，通常 Agent 都需要指定模型配置。

**原因猜测：**
`agentscope` 1.0.15 版本的 `ReActAgent` 初始化参数可能改名了，或者它希望你直接在 `sys_prompt` 或者其他地方处理，又或者它需要一个 `model_config` 对象而不是名字。

**解决计划：**
我将再次使用“读源码”大法，查看 `agentscope/agent/_react_agent.py` 的 `__init__` 签名。

1.  **读取源码**：查看 `_react_agent.py`。
2.  **确认参数名**：也许参数名变成了 `model` 或者 `config_name`？或者它继承自 `ReActAgentBase`，参数在那里面？

只要看一眼源码，一切就都清楚了。我们不猜了，直接看“说明书”。