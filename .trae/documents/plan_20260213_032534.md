The user wants to utilize the empty space in the **Sidebar** (left panel) to display the **Leaderboard** and **Recent Interaction History**, as they are currently hard to find (likely at the bottom of the page).

**Plan:**

1.  **Modify `app.py`**:
    *   Locate the `with st.sidebar:` block.
    *   After the user info and logout button, add a new section: **"🏆 历史排行榜" (History Leaderboard)**.
    *   Fetch match history using `storage.get_match_history`.
    *   Iterate through the history records and display them in the sidebar using `st.expander` to save space while allowing details to be viewed.
    *   Inside each expander:
        *   Show the **Score** and **Partner Name**.
        *   Show the **Verdict/Summary** (final_verdict).
        *   Provide a way to view the chat log (e.g., `st.json` inside the expander for quick access).
    *   This ensures the "Recent Interactions" are always visible regardless of where the user is on the main page.

This directly addresses the user's request to fill the "blank space" with useful history data.
