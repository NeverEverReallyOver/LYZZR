报错 `FATAL: Tenant or user not found` 表明您的 Supabase 项目**不在东京 (Tokyo)** 区域，因此东京的连接池找不到您的项目。

根据常见的区域选择，我将尝试切换到 **新加坡 (Singapore)** 区域。

**解决计划：**
1.  **修改配置**：将 `.streamlit/secrets.toml` 中的 `host` 改为新加坡连接池地址 `aws-0-ap-southeast-1.pooler.supabase.com`。
2.  **重启服务**：重新启动 Streamlit 应用。

如果这次还不行，我将需要您在 Supabase 后台查看一下具体的“Region”或“Connection String”。先试一下新加坡！