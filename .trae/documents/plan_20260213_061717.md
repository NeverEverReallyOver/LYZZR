The user encountered a `NameError: name 'chatted_map' is not defined` in `app.py` at line 237, inside the `render_candidate_selector` function.

**Analysis:**

1.  **Scope Issue**: `chatted_map` is defined in the `main` function (around line 180 in the previous version), but `render_candidate_selector` is defined as a nested function inside `main`.
2.  **Why it fails**:
    *   In the previous refactoring, I moved the definition of `render_candidate_selector` *before* `chatted_map` was defined?
    *   Or, I defined `render_candidate_selector` but it relies on `chatted_map` which is defined *later* or *outside* its scope in a way that Python's closure capture missed it or it wasn't defined yet when called?
    *   Looking at the previous diff: `chatted_map = storage.get_chatted_users(...)` was likely removed or moved.
    *   Wait, in the previous `old_str`, `chatted_map` was defined *before* the candidate loop.
    *   In the `new_str`, I defined `render_candidate_selector` *inside* `main`.
    *   If `chatted_map` is defined *after* `render_candidate_selector` is defined but *before* it is called, it should be fine (closure).
    *   However, if I accidentally removed the line `chatted_map = storage.get_chatted_users(...)` during the SearchReplace, that would cause the error.

**Investigation of Previous Edit:**
I see in the `old_str`:
```python
        # 获取已聊列表
        chatted_map = storage.get_chatted_users(current_user.user_id)
```
In the `new_str`:
```python
        # 布局逻辑重构：分栏显示
        # -----------------------------------------------------------------------------
        
        # 定义渲染嘉宾列表的函数 (支持 grid 和 list 两种模式)
        def render_candidate_selector(mode="grid"):
```
**CRITICAL ERROR**: I removed the line `chatted_map = storage.get_chatted_users(current_user.user_id)` in the previous SearchReplace! I replaced the block containing it with the function definition, but forgot to put the variable definition back in or inside the function.

**Plan:**

1.  **Modify `app.py`**:
    *   Restore `chatted_map = storage.get_chatted_users(current_user.user_id)` inside `main`, ideally before `render_candidate_selector` is defined or at least before it's called.
    *   Better yet, put it inside `render_candidate_selector` so it's always fresh, OR pass it as an argument.
    *   Given it's a closure, defining it in `main` before the function definition is safest.
    *   Actually, just adding it back at the top of the logic block is the fix.

**Fix Details:**
Add the missing line back before `render_candidate_selector` definition.

```python
        # 获取已聊列表 (Restoring this line)
        chatted_map = storage.get_chatted_users(current_user.user_id)

        # 定义渲染嘉宾列表的函数...
```

Wait, `render_candidate_selector` is a local function.
I will check where to insert it.

**Action:**
Insert `chatted_map = storage.get_chatted_users(current_user.user_id)` before `def render_candidate_selector(mode="grid"):`.
