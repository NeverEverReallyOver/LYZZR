The user reports that the system shows them "chatting with themselves" in the leaderboard/history, as seen in the screenshot where the partner name is "程成 (AI)" while the current user is also "程成".

**Analysis:**

1.  **The Screenshot**: Shows "程成 (AI)" in the leaderboard. This implies the partner's name was detected as "程成".
2.  **The Cause**: In `src/storage.py`, the `_enrich_virtual_user_names` method tries to guess the virtual user's name from the `chat_log`.
    *   It extracts all names from the chat log that are not "System".
    *   It then picks the **first** name found: `r['partner_name'] = list(names)[0] + " (AI)"`.
    *   The `names` set likely contains `['程成', 'GuestName']`.
    *   Since sets are unordered (or ordering depends on hash), it might pick "程成" (the user's own name) instead of the partner's name.
    *   Since `match_records` stores `user_b` as the virtual ID (e.g. `guest_123`), and the virtual user's name is only inside the chat log, we need a better way to distinguish "Me" from "Partner".

**Plan:**

1.  **Modify** **`src/storage.py`**:
    *   Update `_enrich_virtual_user_names`.
    *   We need to know the **current user's name** to exclude it from the potential partner names.
    *   However, `get_top_matches` and `get_recent_matches` only take `username` (the ID), not the user's display name.
    *   **Solution**: Fetch the current user's name from the `users` table first (using `username`), OR pass the current user's name into these methods from `app.py`.
    *   Since `app.py` has `current_user.name`, passing it is cleaner.

**Revised Plan:**

1.  Modify `get_top_matches` and `get_recent_matches` in `src/storage.py` to accept an optional `current_user_name` parameter.
2.  In `_enrich_virtual_user_names`, if `current_user_name` is provided, remove it from the `names` set before picking a partner name.
3.  Update `app.py` to pass `current_user.name` when calling these methods.

This ensures we pick the *other* person's name from the chat log.
