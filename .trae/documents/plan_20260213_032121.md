The user wants to bring back virtual users (AI-generated candidates) because the current real user base is small.

I've examined the code:
1.  `src/generator.py` is intact and has `CandidateGenerator` which can generate random profiles with `generate_pool`.
2.  `app.py` already has logic to generate virtual users, but it's conditional: `if len(real_candidates) < 5`. This threshold might be too low, or the user simply wants *more* options regardless of how many real users exist.

**Plan:**

1.  **Modify `app.py`**:
    *   Change the logic to **always** ensure a minimum pool size (e.g., 20 candidates), regardless of how many real users there are.
    *   Instead of `if len(real_candidates) < 5`, use logic like: `if len(real_candidates) < 20: virtual_needed = 20 - len(real_candidates)`.
    *   This guarantees that the user always sees at least 20 candidates, mixing real and virtual ones.
    *   I will also update the UI toast message to be more informative ("Found X real users, generating Y virtual users...").

This will satisfy the user's request to populate the "square" with more candidates, just like in V1.
