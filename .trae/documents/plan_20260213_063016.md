The user reports that the "Leaderboard" on the right side (in the split view) still shows "Self-Chat" names (e.g., "程成 (AI)") and does not match the Sidebar Leaderboard (which I fixed earlier).

**Analysis:**

1.  **The Sidebar Leaderboard** (Left) shows correct names like "上官雪芳 (AI)" and "Stephanie (AI)". This is because `storage.get_top_matches` was updated to take `current_user_name` and filter it out.
2.  **The Right Leaderboard** (Main Content) shows "程成 (AI)". This means it's still using the old logic or calling the storage method *without* passing `current_user_name`.
3.  **Code Check**: In `app.py`, under `# 4. 排行榜 (New)`, I likely call `storage.get_match_history(current_user.user_id)`.
4.  **Issue**: `storage.get_match_history` (the deprecated method) calls `get_top_matches` but **does not pass** `current_user_name`.
    *   In `src/storage.py`:
        ```python
        def get_match_history(self, username: str) -> list[dict]:
            return self.get_top_matches(username, limit=100)
        ```
    *   It's missing the `current_user_name` argument.

**Plan:**

1.  **Modify `src/storage.py`**:
    *   Update `get_match_history` to accept `current_user_name` and pass it to `get_top_matches`.

2.  **Modify `app.py`**:
    *   Update the call to `storage.get_match_history` in the "Right Leaderboard" section to pass `current_user_name=current_user.name`.

This ensures consistency between the sidebar and the main view.
