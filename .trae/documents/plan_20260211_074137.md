根据您的要求，为了确保价值观匹配机制生效，我们需要强制用户完成图灵校准，并确保生成的 AI 也有对应数据。

经过代码分析，发现后端生成逻辑（`src/generator.py`）和评估逻辑（`src/evaluator.py`）已经支持了该功能，**缺口主要在前端验证**。

### 1. 强制用户填写 (`app.py`)
*   **当前问题**：目前图灵校准是选填的，用户留空也能通过。
*   **修改计划**：
    *   在侧边栏表单中，明确标记“图灵校准”的三个问题为 **(必填)**。
    *   在用户点击“开始智能筛选”按钮时，增加拦截逻辑：
        *   检查用户是否回答了全部 3 个问题。
        *   如果未完成，显示 `st.error` 提示“请先完成左侧侧边栏的【图灵校准】必填项”，并阻止程序继续运行。

### 2. 确保 AI 对象数据一致性 (`src/generator.py`)
*   **现状确认**：`CandidateGenerator` 已经内置了为每个 AI 自动生成这 3 个问题答案的逻辑。
*   **微调**：我会再次核对 `app.py`（用户界面）和 `generator.py`（AI 生成）中的问题文本，确保它们**完全一致**（例如“约会迟到” vs “迟到”），以便评估器能准确匹配。

### 3. 预期效果
*   **用户侧**：必须填写自己的价值观偏好。
*   **AI 侧**：每个生成的候选人都会自带一套价值观回答（如“迟到零容忍”或“随和包容”）。
*   **评估侧**：系统将强制对比双方在这些具体问题上的回答，从而精准计算“价值观契合度”。

无需修改 `src/evaluator.py`，现有逻辑已支持此对比。