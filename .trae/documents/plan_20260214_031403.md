I have detected that the local service is already running on port 8501.

You can access the **Love and Agents** (恋与代理人) web interface by clicking the link below:

**[http://localhost:8501](http://localhost:8501)**

If the link doesn't open or shows an error, please try running the following command in your terminal to restart the service:
`streamlit run app.py`
