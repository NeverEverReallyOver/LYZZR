用户希望在**聊天进行时**（无论是批量匹配还是单人聊天），调整页面布局：

1. **保持不变**：“开始批量自动匹配”按钮、“帮我推荐一个”按钮、以及“搜索嘉宾”输入框，依然放在页面最顶部。
2. **上移**：“2. 深度聊天室”、“3. 最终裁判报告”以及“再来一次”按钮，移动到搜索框下方。
3. **下移**：“1. 嘉宾广场”（即 20 个嘉宾的列表），移动到页面最底部。

**分析：**
目前 `app.py` 是按照代码顺序线性渲染的。要实现这个需求，我们需要根据当前的状态（`chat_active` 或 `batch_processing`）来决定渲染顺序。

**实现方案：**

1. **封装嘉宾广场渲染逻辑**：将渲染 20 个嘉宾卡片的代码提取为一个独立的函数 `render_candidate_grid(candidates, chatted_map)`，以便在不同位置调用。
2. **重构** **`main`** **函数布局**：

   * **第一部分（固定头部）**：渲染标题、两个操作按钮、搜索框。

   * **第二部分（条件渲染）**：

     * 判断当前是否处于“活跃状态”（`st.session_state.get('batch_processing')` 或 `st.session_state.chat_active`）。

     * **如果是活跃状态**：

       1. 先渲染“活跃内容”（批量进度条 / 深度聊天室 / 裁判报告）。
       2. 添加

