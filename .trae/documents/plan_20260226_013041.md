经过检查，您的本地服务目前**已经停止运行**（端口 8501 未被占用）。这可能是因为终端窗口关闭、意外报错退出或者电脑重启导致的。

**解决方案：**

我将为您重新启动服务。

1.  **执行命令**：运行 `streamlit run app.py` 启动应用程序。
2.  **访问地址**：启动成功后，您可以通过浏览器访问 `http://localhost:8501`。

请确认执行此计划。
