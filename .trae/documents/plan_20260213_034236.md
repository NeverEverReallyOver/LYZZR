The user wants to decouple the "Leaderboard" from the "Chat History" in the sidebar.
Currently, they are combined (showing history sorted by score).

**User Requirements:**

1. **Leaderboard**: Only show the **top 3** highest-scoring matches.
2. **History**: Show the **last 5** recent conversations (sorted by time, presumably).

**Analysis of** **`storage.py`:**

* `get_match_history` currently returns *all* matches sorted by score (`ORDER BY r.match_score DESC`).

* This function is used for the current leaderboard.

**Plan:**

1. **Modify** **`src/storage.py`**:

   * Create a new method `get_recent_history(username, limit=5)`:

     * Query `match_records` sorted by `created_at DESC` (assuming there's a `created_at` column, checking schema now).

     * Wait, `schema.sql` (from context) or `app.py` usually implies a timestamp.

     * In `get_match_history` logic (lines 165+), I see `r.created_at`.

     * So the query will be `ORDER BY r.created_at DESC LIMIT :limit`.

   * Modify `get_match_history` (or create `get_leaderboard`) to keep `ORDER BY r.match_score DESC` but add `LIMIT 3`.

2. **Modify** **`app.py`**:

   * In the sidebar section:

   * **Section 1: 🏆 排行榜 (Top 3)**

     * Call `storage.get_leaderboard(current_user.user_id, limit=3)` (or modify existing call).

     * Display the top 3 scores.

   * **Section 2: 🕒 最近对话 (History)**

     * Call `storage.get_recent_history(current_user.user_id, limit=5)`.

     * Display the last 5 chats (Partner Name + Time + View Button).

**Refined Plan Details:**

* I'll rename `get_match_history` to `get_leaderboard` (or keep it and add limit) to make it clear.

* Actually, `get_match_history` is a good name for history. I should make a new specific function `get_top_matches` for the leaderboard.

* And `get_recent_matches` for history.

* I will verify `created_at` column exists. (In `app.py` line 405: `record['created_at']` is used, so it exists).

**Action Items:**

1. Update `src/storage.py`: Add `get_top_matches` and `get_recent_matches`.
2. Update `app.py`: Split sidebar into two distinct sections.

